// �2023 Nathan Greenfield
#include <iostream>
#include <string>

#include "towers.h"

int main(int argc, const char* argv[])
{
    /*
    // testing disk
    GWindow gw (500, 500);
    Disk t = Disk(200, 100, 200, 100);
    t.draw(gw);
    pause(3000);
     */
    
/*
    // testing peg & drawing multiple pegs
    GWindow gw (1000, 500);
    
    Disk t = Disk(200, 100, 70, 50);
    Disk u = Disk(300, 100, 60, 50);
    Peg p1 = Peg(250, 500, 10, 500);
    
    p1.addDisk(t);
    p1.addDisk(u);
    //p1.removeDisk();
    
    Disk a = Disk(200, 100, 80, 50);
    Disk b = Disk(200, 100, 70, 50);
    Peg p2 = Peg(500, 400, 10, 400);
    
    p2.addDisk(a);
    p2.addDisk(b);
    
    Disk y = Disk(200, 100, 100, 50);
    Disk z = Disk(200, 100, 90, 50);
    Peg p3 = Peg(750, 400, 10, 400);
    
    p3.addDisk(y);
    p3.addDisk(z);
    
    std::vector<Peg> pegs;
    pegs.push_back(p1);
    pegs.push_back(p2);
    pegs.push_back(p3);
    
    draw(gw, pegs, 5000);
    
    //pause(5000);
 */
     
    
     //UNCOMMENT THIS!!!!!
	towerRun();
    

	return 0;
}
